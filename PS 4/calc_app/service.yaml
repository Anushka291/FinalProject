apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort
  selector:
    app: flask-app   # ✅ MUST match deployment label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3002   # ✅ matches containerPort
      nodePort: 30000    # access via browser