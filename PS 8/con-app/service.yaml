apiVersion: v1
kind: Service
metadata:
  name: con-app-service
spec:
  type: NodePort
  selector:
    app: con-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7000
      nodePort: 30004